spring:
  datasource:
    url: jdbc:h2:~/moneyflow
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
---
